// Placeholder funkce pro skenování etiket
function scanLabel() {
  alert("📷 OCR čtečka zatím není aktivní – brzy doplníme.");
}

// Zatím jen inicializace mapového místa
document.addEventListener("DOMContentLoaded", () => {
  document.getElementById("map").textContent = "🗺️ Mapa (Mapy.cz) bude zde.";
});
